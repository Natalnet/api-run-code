#include <stdio.h>
#include <math.h>

int main(){
    int a, b, c;
    scanf("%i %i %i", &a, &b, &c);
    printf("a=%d b=%d c=%d\n", a, b, c);
    printf("%lf\n", sqrt(a));
    printf("%lf\n", pow(b,c));
    while(1){
        printf("é nois!\n");
    }
    return -1;
}